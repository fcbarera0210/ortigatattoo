---
interface Props {
  title?: string;
  subtitle?: string;
}

const {
  title = 'Tu próxima pieza empieza acá',
  subtitle = 'Reservá tu sesión o elegí un diseño flash. Te confirmamos por WhatsApp.',
} = Astro.props;
---

<section class="mx-auto max-w-6xl px-4 py-16 md:px-6">
  <div class="gothic-frame-crimson px-6 py-14 text-center md:px-12">
    <p class="section-eyebrow mb-4">Reservas</p>
    <h2 class="mx-auto max-w-2xl font-display text-3xl tracking-[0.1em] md:text-4xl">{title}</h2>
    <div class="divider-crimson mx-auto mt-6"></div>
    <p class="mx-auto mt-4 max-w-xl text-muted">{subtitle}</p>
    <div class="mt-8 flex flex-wrap justify-center gap-4">
      <a href="/reservar" class="btn-primary">Reservar ahora</a>
      <a href="/disponibles" class="btn-secondary">Ver disponibles</a>
    </div>
  </div>
</section>
